{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "escape": { "type": "stdio", "command": "../outside", "cwd": "${PLUGIN_DATA}/tmp" },
    "good": { "type": "stdio", "command": "tool" }
  }
}
